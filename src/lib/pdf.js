export async function exportReportPDF({report,rows}){ const txt=`CORE v11 - Rapporto ${report?.day||''}\nTotale ore: ${rows.reduce((a,b)=>a+(b.hours||0),0)}`; const blob=new Blob([txt],{type:'text/plain'}); const url=URL.createObjectURL(blob); const a=document.createElement('a'); a.href=url; a.download=`rapporto-${report?.day||'oggi'}.txt`; a.click(); URL.revokeObjectURL(url);}
